# Public Supabase project settings. The anon key is safe for browser use when RLS is correct.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Server-only. Configure in Vercel; never commit the real value.
SUPABASE_SERVICE_ROLE_KEY=
