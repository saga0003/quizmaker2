name: Record F8 progress heartbeat
on:
  push:
    branches: [phase1-hardening]
    paths: ['.github/workflows/phase1-f8-heartbeat.yml']
permissions:
  contents: write
jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase1-hardening
          fetch-depth: 0
      - name: Record heartbeat
        shell: bash
        run: |
          cat >> PHASE1_PROGRESS_LOG.md <<'EOF'

## 2026-09-01 17:59:04 IST — F8 hierarchy audit / test-first heartbeat
- **Run start/end:** 17:59:04 IST → approximately 18:09 IST.
- **Active engineering span:** ~10 minutes; the 45–50 minute target was not met because this execution window ended earlier; no padding.
- **Section worked:** F — Analytics, F8.
- **Checklist completed:** none; F8 remains unchecked.
- **Work:** inspected checklist/progress/head/CI and traced institution analytics UI/API/types. Confirmed current School→combined class hierarchy lacks first-class programme and grade steps. Added 15-point test-first acceptance `scripts/f8-institution-drilldown-smoke.mjs` in commit `339c0de4c4781c01a44a3548df52f81a90d6c0a9`, covering programme→grade→section plus downstream subject/chapter/student drill-down.
- **Pending:** identify authoritative programme source; implement programme/grade/section payloads + API snapshots + UI breadcrumbs; pass F8 smoke; wire permanent gate; full release-gate PASS; then check F8.
- **Starting head:** `bbe4e558a9ca36f2409d396a913d0b0523892bee`.
- **Commits:** `339c0de4c4781c01a44a3548df52f81a90d6c0a9` plus heartbeat commits.
- **CI:** latest verified pre-run gate `33505618386` on starting head PASS (~2m18s). New test-first commit is not a verified F8 candidate yet.
- **Vercel/Supabase:** production not promoted or mutated; prior healthy production checkpoint preserved. No Supabase mutation in this audit; canonical programme source must be verified before schema/API assumptions.
- **Rework/blockers:** direct container GitHub DNS unavailable; connected GitHub API used. Accidental empty `f8-work*` branches contain no product changes and are not release candidates. No product blocker.
- **Section progress:** F 7/13 = 53.8% verified.
- **Overall Phase 1:** 65/129 = 50.4% verified.
- **Exact next action:** verify programme source, implement School→programme→grade→section→subject→chapter→topic→student end-to-end, make F8 regression pass, wire it into the complete gate, then verify/check F8.
EOF
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add PHASE1_PROGRESS_LOG.md
          git commit -m 'docs(f8): record hierarchy audit heartbeat [skip ci]' || exit 0
          git push origin HEAD:phase1-hardening
