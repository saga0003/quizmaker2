name: Apply canonical F6 F7 analytics thresholds
on:
  push:
    branches:
      - phase1-hardening
    paths:
      - .github/workflows/c7-record-progress.yml
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase1-hardening
          fetch-depth: 0
      - name: Append Phase 1 heartbeat
        run: |
          cat >> PHASE1_PROGRESS_LOG.md <<'EOF'

## 2026-09-02 06:01:10–06:11:22 IST — J3 blocker / real-school and load acceptance preparation

- **Run start:** 2026-09-02 06:01:10 IST.
- **Run end:** 2026-09-02 06:11:22 IST.
- **Active engineering minutes / best observable engineering span:** approximately **10m12s** from initial branch/CI/production/database inspection through acceptance-tooling implementation, safety regression setup and final recorder staging. The requested 45–50 minute target was **not met because this execution window ended materially earlier**; no time was artificially padded.
- **Section worked:** J3 external legal sign-off blocker, followed by preparation for real-school acceptance and load acceptance while J3 remains externally blocked.
- **Checklist items completed:** none. J3 remains intentionally unchecked pending qualified legal approval; R1–R18 and L1–L6 remain unexecuted and unchecked.
- **Items still pending:** J3; R1–R18 real-school acceptance; L1–L6 load acceptance; Z2–Z8 production sign-off.
- **Branch head at run start:** `8ab586edb457e0e131e7e1e3e0779b9b68bacdf3`.
- **Commits created:** `dffdd04356464d29d6eee74e4472fd4a6947701c` deterministic L1–L3 synthetic fixture generator; `030c48f81f377b2ce5a8f51dbd0e8c9608f89629` fail-closed non-production acceptance preflight; acceptance tooling smoke regression; `a1894fb7e3e9fe4e8ea98f2ab1d21453ee6e4cc1` permanent Acceptance Tooling Contract workflow; plus `PHASE1_LOAD_ACCEPTANCE_PROTOCOL.md`. Temporary heartbeat recorder `phase1-run12-heartbeat.yml` did not execute on the non-default branch and was removed cleanly in `72e34656dc47a1d9e33aa7586f7259b867ac6325` before this canonical recorder was used.
- **CI run and result:** previously pending complete release-gate run `33571618349` was superseded; parent complete release gate `33571729592` is confirmed **PASS** on the last fully verified product candidate. Fresh full release-gate verification was triggered by the new acceptance-tooling commits and remained in progress/superseded during this short execution window, so no new checklist credit is claimed from preparation work. The Acceptance Tooling Contract was also triggered and must be confirmed green before treating the tooling itself as release-ready.
- **Vercel state:** permanent production remained protected and unchanged. Production `error`/`fatal` runtime inspection for the preceding 24 hours was clean at run start; hardening previews were inspected and no production promotion was attempted.
- **Supabase state:** connected projects `quizmaker` (`xzfozpnzvznqrvcsoail`) and `Final Viva Project` (`jjkixgpddjognznrnqja`) remained `ACTIVE_HEALTHY`; no database mutation occurred. Security Advisor surfaced leaked-password protection as disabled plus existing SECURITY DEFINER/compatibility-RPC warnings. The RPC warnings require authorization-context review rather than blanket revocation because Phase-1 compatibility RPCs are intentional; leaked-password protection remains an explicit security follow-up before final production sign-off.
- **Rework/failures/blockers:** J3 remains an external qualified-counsel blocker. R1–R18 require an authorised real institution dataset/operators; L1–L6 require a dedicated non-production acceptance tenant and actual measured execution. Production was not used for load testing. One newly-created heartbeat workflow did not execute because newly introduced non-default-branch workflows are not reliably registered; it was removed rather than left as CI debris, and this already-registered recorder was used instead.
- **Acceptance preparation delivered:** `PHASE1_LOAD_ACCEPTANCE_PROTOCOL.md` now defines fail-closed evidence and pass criteria for L1–L6; deterministic synthetic fixtures generate exactly 2,000 students, 50,000 questions and 1,000 papers without PII; the acceptance preflight refuses known production/main hosts and requires explicit non-production acknowledgement; a permanent smoke regression checks those safety contracts.
- **Active section completion:** Section J remains **4/5 = 80%** verified; real-school acceptance remains **0/18**; load acceptance remains **0/6**.
- **Overall Phase 1:** remains **96/129 = 74.4%** verified.
- **Exact next action:** confirm the complete release gate and Acceptance Tooling Contract on the exact acceptance-tooling head; record qualified counsel approval against `PHASE1_LEGAL_REVIEW_PACKET.md` without weakening J3; when authorised operators/data are available execute R1–R18 exactly per `PHASE1_REAL_SCHOOL_ACCEPTANCE_PROTOCOL.md`; then provision a dedicated non-production load tenant, run the fail-closed preflight and deterministic fixtures, execute L1–L6 with measured evidence, re-run the complete release gate, resolve leaked-password protection before final security sign-off, and only then proceed through Z2–Z8 production sign-off.
EOF
      - name: Restore recorder and commit heartbeat
        run: |
          git config user.name "Evidara Hardening Bot"
          git config user.email "sagar@smis.edu.in"
          git checkout HEAD^ -- .github/workflows/c7-record-progress.yml
          git add PHASE1_PROGRESS_LOG.md .github/workflows/c7-record-progress.yml
          git commit -m "docs: record acceptance-prep heartbeat [skip ci]"
          git push origin HEAD:phase1-hardening
