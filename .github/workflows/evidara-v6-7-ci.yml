name: Archived Evidara V6.8 Production CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived historical workflow. Use Evidara V11.2 QA for the active baseline."
