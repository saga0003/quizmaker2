#!/usr/bin/env node
import { mkdir, writeFile } from 'node:fs/promises';
import { chromium } from 'playwright';
import { createClient } from '@supabase/supabase-js';
const REQUIRED_ACK='YES_I_UNDERSTAND_NON_PRODUCTION_ONLY';
const LOCAL_ACK='YES_LOCAL_SYNTHETIC_ACCEPTANCE_ONLY';
const EXPECTED_ORG='evidara-school-acceptance';
const EXPECTED_PAPER='e5801a88-1e7f-4b4f-a715-ad44ce2b3c43';
const EXPECTED_PAPER_TITLE='Phase 1 R8 Physics Acceptance Test';
const EXPECTED_ATTEMPT='134ddbe2-bc9f-4863-9aba-3b9def08d69e';
const EVIDENCE_DIR=process.env.EVIDARA_ACCEPTANCE_EVIDENCE_DIR||'acceptance-evidence/r12-held-result';
function need(name){const v=(process.env[name]||'').trim();if(!v)throw new Error(`Missing ${name}`);return v;}
function target(raw){const u=new URL(raw);if(u.protocol!=='http:'||!['127.0.0.1','localhost'].includes(u.hostname.toLowerCase()))throw new Error('R12 localhost only');if(process.env.EVIDARA_ACCEPTANCE_LOCALHOST!==LOCAL_ACK)throw new Error('R12 localhost acknowledgement missing');return u.origin;}
async function heldContract(email,password){const c=createClient(need('NEXT_PUBLIC_SUPABASE_URL'),need('NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY'),{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}});const {error:a}=await c.auth.signInWithPassword({email,password});if(a)throw new Error(`R12 synthetic login failed: ${a.message}`);try{const {data,error}=await c.rpc('list_my_attempt_results');if(error)throw new Error(error.message);const r=(Array.isArray(data)?data:[]).find(x=>x?.attempt_id===EXPECTED_ATTEMPT);if(!r||r.paper_id!==EXPECTED_PAPER||r.paper_title!==EXPECTED_PAPER_TITLE)throw new Error('Expected isolated R12 attempt missing');if(r.result_mode!=='hidden'||r.result_release_level!=='none'||r.result_released!==false)throw new Error('Held release contract mismatch');for(const k of ['score','maximum_marks','percentage','correct_count','incorrect_count','unanswered_count'])if(r[k]!==null)throw new Error(`Held result leaked ${k}`);if(r.answers_released!==false||r.analytics_released!==false)throw new Error('Held result leaked detail release');return r;}finally{await c.auth.signOut().catch(()=>{});}}
async function waitStudent(page){await page.waitForFunction(()=>{const u=new URL(window.location.href),v=u.searchParams.get('view')||'';return !document.body.innerText.includes('Sign in to Evidara')&&(v==='student-dashboard'||v.startsWith('student-'));},null,{timeout:30000});}
async function main(){if(process.env.EVIDARA_LOAD_ACCEPTANCE!==REQUIRED_ACK)throw new Error('R12 acknowledgement missing');if((process.env.EVIDARA_ACCEPTANCE_ORG_SLUG||'').trim()!==EXPECTED_ORG)throw new Error('R12 synthetic tenant only');const t=target(need('EVIDARA_ACCEPTANCE_URL')),email=need('EVIDARA_ACCEPTANCE_STUDENT_EMAIL'),password=need('EVIDARA_ACCEPTANCE_STUDENT_PASSWORD');await mkdir(EVIDENCE_DIR,{recursive:true});const contract=await heldContract(email,password);const browser=await chromium.launch({headless:true}),context=await browser.newContext({viewport:{width:1366,height:900}}),page=await context.newPage();const consoleErrors=[],pageErrors=[],failedResponses=[];let baseline=false;page.on('console',m=>{if(baseline&&m.type()==='error')consoleErrors.push(m.text().slice(0,500));});page.on('pageerror',e=>{if(baseline)pageErrors.push(String(e.message||e).slice(0,500));});page.on('response',r=>{if(!baseline||r.status()<400)return;const u=new URL(r.url());failedResponses.push({status:r.status(),method:r.request().method(),url:`${u.origin}${u.pathname}`});});try{await page.goto(`${t}/?view=login`,{waitUntil:'networkidle',timeout:45000});await page.locator('#email').fill(email);await page.locator('#password').fill(password);await page.getByRole('button',{name:/^Sign In$/}).click();await waitStudent(page);baseline=true;await page.goto(`${t}/?view=student-results`,{waitUntil:'networkidle',timeout:45000});await page.getByRole('heading',{name:'Results'}).waitFor({state:'visible',timeout:30000});await page.getByText(EXPECTED_PAPER_TITLE,{exact:false}).first().waitFor({state:'visible',timeout:30000});await page.getByText('Result held',{exact:true}).first().waitFor({state:'visible',timeout:10000});await page.getByText('Details available after release',{exact:true}).first().waitFor({state:'visible',timeout:10000});const body=await page.locator('body').innerText();if(body.includes('8/80')||body.includes('10%')||body.includes('2 correct'))throw new Error('Rendered held result leaked authoritative marks');if(await page.getByRole('button',{name:/Continue reflection/}).count())throw new Error('Held result exposed reflection');await page.screenshot({path:`${EVIDENCE_DIR}/01-held-result.png`,fullPage:true});const manifest={result:'PASS',acceptanceItem:'R12-held',target:t,executionSurface:'branch-checkout-localhost',organizationSlug:EXPECTED_ORG,paperId:EXPECTED_PAPER,attemptId:EXPECTED_ATTEMPT,resultMode:contract.result_mode,resultReleaseLevel:contract.result_release_level,resultReleased:contract.result_released,scoreFieldsWithheld:true,renderedPaperVisible:true,renderedHeldStateVisible:true,renderedScoreWithheld:true,reflectionSuppressed:true,productionProtected:true,secretsRecorded:false,unexpectedConsoleErrorCount:consoleErrors.length,pageErrorCount:pageErrors.length,failedResponseCount:failedResponses.length,capturedAt:new Date().toISOString()};await writeFile(`${EVIDENCE_DIR}/r12-held-results.json`,`${JSON.stringify(manifest,null,2)}\n`);console.log(JSON.stringify(manifest,null,2));if(consoleErrors.length||pageErrors.length||failedResponses.length)throw new Error(`R12 held page errors ${consoleErrors.length}/${pageErrors.length}/${failedResponses.length}`);}finally{await context.close();await browser.close();}}
main().catch(e=>{console.error(`R12 HELD RESULT ACCEPTANCE FAILED: ${e instanceof Error?e.message:String(e)}`);process.exit(1);});