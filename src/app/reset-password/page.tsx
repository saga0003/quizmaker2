"use client";
import { useState } from "react";
import Link from "next/link";
import { Logo } from "@/components/Logo";
import { supabase } from "@/lib/supabase";
export default function ResetPassword(){const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [message,setMessage]=useState("");const [error,setError]=useState(false);const [busy,setBusy]=useState(false);async function save(e:React.FormEvent){e.preventDefault();setError(false);setMessage("");if(password!==confirm){setError(true);setMessage("Passwords do not match.");return}if(!supabase){setMessage("Demo Mode: password reset requires Supabase.");return}setBusy(true);const {error:e2}=await supabase.auth.updateUser({password});if(e2){setError(true);setMessage(e2.message)}else setMessage("Password updated. You can now return to your dashboard.");setBusy(false)}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20}}><div className="rm-card" style={{padding:24,maxWidth:460,width:"100%"}}><Link href="/" style={{display:"inline-block",marginBottom:20}}><Logo/></Link><h1 style={{color:"#131e35"}}>Choose a new password</h1><form onSubmit={save} style={{display:"grid",gap:14}}><label><span className="rm-label">New password</span><input type="password" minLength={8} className="rm-input" value={password} onChange={e=>setPassword(e.target.value)} required/></label><label><span className="rm-label">Confirm password</span><input type="password" minLength={8} className="rm-input" value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label><button disabled={busy} className="rm-btn-primary">{busy?"Updating…":"Update password"}</button></form>{message&&<div style={{marginTop:14,padding:12,borderRadius:10,background:error?"#fef3f2":"#ecfdf3",color:error?"#b42318":"#137a3a"}}>{message}</div>}</div></main>}
