import { LockKeyhole, ShieldCheck } from "lucide-react";

export function BenchmarkPrivacyNotice(){return <div className="so-notice info"><LockKeyhole/><span><strong>Private by design:</strong> each school can see only its own named students. Every external student and school remains anonymous. Shared results contain aggregate evidence only and remain hidden until the privacy minimum is reached.</span><ShieldCheck/></div>}
