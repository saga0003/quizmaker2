/* ScholarOS Version 2 — persistent pilot workflows */
(() => {
  const DB_KEY = 'scholaros-v2-db';
  const SESSION_KEY = 'scholaros-v2-session';

  const seedQuestions = [
    {id:'q1',subject:'Science',topic:'Motion',difficulty:'Medium',skill:'Application',expectedTime:75,errorType:'Conceptual',text:'A cyclist travels 120 m in 20 seconds and then 180 m in 30 seconds. What is the average speed for the entire journey?',options:['5 m/s','6 m/s','7.5 m/s','10 m/s'],answer:1,explanation:'Total distance is 300 m and total time is 50 s, so average speed = 300/50 = 6 m/s.',status:'Calibrated',discrimination:0.46},
    {id:'q2',subject:'Biology',topic:'Cell Structure',difficulty:'Easy',skill:'Recall',expectedTime:40,errorType:'Recall',text:'Which cell organelle controls most activities of the cell and contains genetic material?',options:['Mitochondrion','Vacuole','Nucleus','Ribosome'],answer:2,explanation:'The nucleus stores genetic material and regulates the cell’s activities.',status:'Calibrated',discrimination:0.39},
    {id:'q3',subject:'Mathematics',topic:'Ratio',difficulty:'Medium',skill:'Analysis',expectedTime:95,errorType:'Procedural',text:'The ratio of boys to girls in a class is 3:5. If 8 more boys join, the ratio becomes 11:15. How many students were originally in the class?',options:['32','40','48','64'],answer:3,explanation:'Let boys = 3x and girls = 5x. (3x+8)/(5x)=11/15 gives x=8, hence total = 8x = 64.',status:'Review',discrimination:0.28},
    {id:'q4',subject:'Reasoning',topic:'Data Interpretation',difficulty:'Hard',skill:'Analysis',expectedTime:60,errorType:'Interpretation',text:'A test score rises by 20%, then falls by 20%. Compared with the original score, the final score is:',options:['4% lower','Unchanged','4% higher','8% lower'],answer:0,explanation:'1.20 × 0.80 = 0.96, so the final value is 4% below the original.',status:'Calibrated',discrimination:0.51},
    {id:'q5',subject:'Chemistry',topic:'Matter',difficulty:'Medium',skill:'Understanding',expectedTime:50,errorType:'Interpretation',text:'Which observation most strongly indicates that a chemical change has taken place?',options:['Ice melting','Water boiling','A gas forming with a colour change','Sugar dissolving'],answer:2,explanation:'Gas formation together with a colour change is strong evidence that new substances formed.',status:'Calibrated',discrimination:0.43}
  ];

  const defaultDb = {
    version: 2,
    organisations:[{id:'org1',name:'ScholarOS Learning Network'}],
    schools:[
      {id:'sch1',name:'Green Valley School',city:'Chikmagalur',board:'CBSE',grades:'8–10',students:6,status:'Active',coordinator:'Meera N.',consent:83,createdAt:'2026-06-12'},
      {id:'sch2',name:'Horizon Public School',city:'Hassan',board:'ICSE',grades:'9–12',students:2,status:'Pilot',coordinator:'Ravi K.',consent:75,createdAt:'2026-07-02'},
      {id:'sch3',name:'Vidya Bharathi Academy',city:'Shivamogga',board:'State',grades:'8–12',students:1,status:'Active',coordinator:'Sahana P.',consent:88,createdAt:'2026-05-25'}
    ],
    users:[
      {id:'u1',name:'Hema Sagar',email:'super@scholaros.in',password:'demo123',role:'super',schoolId:null},
      {id:'u2',name:'Programme Manager',email:'admin@scholaros.in',password:'demo123',role:'admin',schoolId:null},
      {id:'u3',name:'Green Valley Admin',email:'school@greenvalley.edu',password:'demo123',role:'school',schoolId:'sch1'},
      {id:'u4',name:'Ananya Rao',email:'ananya@student.in',password:'demo123',role:'student',schoolId:'sch1',studentId:'st1'}
    ],
    students:[
      {id:'st1',name:'Ananya Rao',grade:'10',section:'A',schoolId:'sch1',parent:'Rashmi Rao',phone:'9000000001',interest:'NEET',status:'Active'},
      {id:'st2',name:'Arjun M.',grade:'9',section:'A',schoolId:'sch1',parent:'Mahesh M.',phone:'9000000002',interest:'Undecided',status:'Active'},
      {id:'st3',name:'Sana F.',grade:'10',section:'B',schoolId:'sch1',parent:'Fathima S.',phone:'9000000003',interest:'JEE',status:'Active'},
      {id:'st4',name:'Kiran P.',grade:'8',section:'A',schoolId:'sch1',parent:'Prakash P.',phone:'9000000004',interest:'Olympiad',status:'Active'},
      {id:'st5',name:'Nidhi S.',grade:'10',section:'A',schoolId:'sch1',parent:'Shilpa S.',phone:'9000000005',interest:'NEET',status:'Active'},
      {id:'st6',name:'Rahul K.',grade:'9',section:'B',schoolId:'sch1',parent:'Kavitha K.',phone:'9000000006',interest:'Undecided',status:'Active'},
      {id:'st7',name:'Meghana K.',grade:'12',section:'A',schoolId:'sch2',parent:'Kiran K.',phone:'9000000007',interest:'NEET repeat',status:'Active'},
      {id:'st8',name:'Rohan S.',grade:'10',section:'A',schoolId:'sch2',parent:'Suresh S.',phone:'9000000008',interest:'Undecided',status:'Active'},
      {id:'st9',name:'Diya R.',grade:'8',section:'A',schoolId:'sch3',parent:'Rekha R.',phone:'9000000009',interest:'Olympiad',status:'Active'}
    ],
    questions: seedQuestions,
    assessments:[
      {id:'as1',name:'Grade 10 Science Pathway Benchmark',grade:'10',type:'Foundation',mode:'CBT',duration:25,status:'Live',schoolIds:['sch1','sch2'],questionIds:['q1','q2','q3','q4','q5'],createdAt:'2026-07-15'},
      {id:'as2',name:'Grade 9 Foundation Challenge 02',grade:'9',type:'Olympiad',mode:'CBT',duration:30,status:'Scheduled',schoolIds:['sch1','sch2'],questionIds:['q1','q3','q4','q5'],createdAt:'2026-07-16'},
      {id:'as3',name:'NEET Full Syllabus Mock 04',grade:'12',type:'NEET',mode:'CBT + OMR',duration:180,status:'Completed',schoolIds:['sch2'],questionIds:['q1','q2','q3','q4','q5'],createdAt:'2026-07-10'}
    ],
    attempts:[
      {id:'at1',assessmentId:'as1',studentId:'st1',submittedAt:'2026-06-18T10:30:00Z',score:60,correct:3,total:5,percentile:71,avgTime:74,answers:{q1:1,q2:2,q3:1,q4:0,q5:1},times:{q1:78,q2:34,q3:122,q4:67,q5:70}},
      {id:'at2',assessmentId:'as1',studentId:'st1',submittedAt:'2026-07-02T10:30:00Z',score:80,correct:4,total:5,percentile:83,avgTime:66,answers:{q1:1,q2:2,q3:3,q4:1,q5:2},times:{q1:73,q2:31,q3:92,q4:74,q5:60}},
      {id:'at3',assessmentId:'as1',studentId:'st1',submittedAt:'2026-07-12T10:30:00Z',score:80,correct:4,total:5,percentile:92,avgTime:61,answers:{q1:1,q2:2,q3:3,q4:0,q5:0},times:{q1:62,q2:28,q3:86,q4:55,q5:74}},
      {id:'at4',assessmentId:'as1',studentId:'st2',submittedAt:'2026-07-12T11:30:00Z',score:60,correct:3,total:5,percentile:74,avgTime:79,answers:{q1:1,q2:0,q3:3,q4:0,q5:1},times:{q1:82,q2:46,q3:101,q4:72,q5:94}},
      {id:'at5',assessmentId:'as1',studentId:'st3',submittedAt:'2026-07-12T11:35:00Z',score:60,correct:3,total:5,percentile:68,avgTime:69,answers:{q1:0,q2:2,q3:3,q4:0,q5:1},times:{q1:67,q2:35,q3:82,q4:59,q5:102}},
      {id:'at6',assessmentId:'as1',studentId:'st5',submittedAt:'2026-07-12T11:40:00Z',score:100,correct:5,total:5,percentile:97,avgTime:58,answers:{q1:1,q2:2,q3:3,q4:0,q5:2},times:{q1:58,q2:25,q3:78,q4:52,q5:77}}
    ],
    consents:[
      {studentId:'st1',assessment:true,academic:true,counselling:true,verifiedAt:'2026-07-05T09:30:00Z'},
      {studentId:'st2',assessment:true,academic:true,counselling:false,verifiedAt:'2026-07-05T09:33:00Z'},
      {studentId:'st3',assessment:true,academic:false,counselling:true,verifiedAt:'2026-07-05T09:36:00Z'},
      {studentId:'st4',assessment:true,academic:true,counselling:false,verifiedAt:'2026-07-05T09:38:00Z'},
      {studentId:'st5',assessment:true,academic:true,counselling:true,verifiedAt:'2026-07-05T09:41:00Z'},
      {studentId:'st6',assessment:true,academic:false,counselling:false,verifiedAt:'2026-07-05T09:44:00Z'},
      {studentId:'st7',assessment:true,academic:true,counselling:true,verifiedAt:'2026-07-05T10:10:00Z'},
      {studentId:'st8',assessment:true,academic:true,counselling:true,verifiedAt:'2026-07-05T10:14:00Z'}
    ],
    opportunities:[
      {id:'op1',studentId:'st1',stage:'Counselling requested',owner:'Unassigned',nextAction:'Book parent call',createdAt:'2026-07-08'},
      {id:'op2',studentId:'st3',stage:'Report shared',owner:'Programme Manager',nextAction:'Discuss JEE pathway',createdAt:'2026-07-09'},
      {id:'op3',studentId:'st5',stage:'Scholarship eligible',owner:'Unassigned',nextAction:'Invite to advanced cohort',createdAt:'2026-07-10'},
      {id:'op4',studentId:'st7',stage:'Mentor review',owner:'NEET Counsellor',nextAction:'Long-term readiness call',createdAt:'2026-07-11'}
    ],
    interventions:[
      {id:'in1',schoolId:'sch1',title:'Ratio and proportional reasoning',studentIds:['st2','st3'],status:'Open',retestDate:'2026-07-25'},
      {id:'in2',schoolId:'sch1',title:'Scientific data interpretation',studentIds:['st2','st6'],status:'Open',retestDate:'2026-07-28'}
    ]
  };

  function clone(value){ return JSON.parse(JSON.stringify(value)); }
  function loadDb(){
    try {
      const saved = JSON.parse(localStorage.getItem(DB_KEY));
      if(saved && saved.version === 2) return saved;
    } catch(e) { console.warn('ScholarOS local data reset', e); }
    const fresh = clone(defaultDb);
    localStorage.setItem(DB_KEY, JSON.stringify(fresh));
    return fresh;
  }
  function saveDb(){ localStorage.setItem(DB_KEY, JSON.stringify(state.db)); }
  function uid(prefix){ return prefix + Date.now().toString(36) + Math.random().toString(36).slice(2,6); }
  function session(){
    try { return JSON.parse(localStorage.getItem(SESSION_KEY)); } catch(e) { return null; }
  }
  function setSession(user){
    state.session = user ? {id:user.id,name:user.name,email:user.email,role:user.role,schoolId:user.schoolId||null,studentId:user.studentId||null} : null;
    if(user) localStorage.setItem(SESSION_KEY, JSON.stringify(state.session)); else localStorage.removeItem(SESSION_KEY);
  }

  state.db = loadDb();
  state.session = session();
  state.role = state.session?.role || localStorage.getItem('scholaros-role') || 'super';
  state.page = 'dashboard';
  state.activeAssessmentId = 'as1';
  state.testIndex = 0;
  state.answers = {};
  state.answerTimes = {};
  state.questionStartedAt = Date.now();
  state.testComplete = false;
  state.lastAttemptId = null;

  roleConfig.super = {label:'Super Admin',subtitle:'Platform command centre',nav:[['dashboard','Command Centre','⌂'],['schools','Schools','▦'],['assessments','Assessments','✓'],['questions','Question Bank','▤'],['intelligence','Lead Intelligence','◎'],['consent','Consent & Privacy','⚿'],['reports','Exports & Reports','↧'],['roadmap','Product Roadmap','↗']]};
  roleConfig.admin = {label:'Organisation Admin',subtitle:'Programme operations',nav:[['dashboard','Operations','⌂'],['assessments','Assessment Builder','✓'],['questions','Question Bank','▤'],['students','Student Directory','♙'],['intelligence','Performance Segments','◎'],['reports','Reports','↧']]};

  function schoolName(id){ return state.db.schools.find(s=>s.id===id)?.name || '—'; }
  function currentStudent(){ return state.db.students.find(s=>s.id===(state.session?.studentId || 'st1')) || state.db.students[0]; }
  function assessment(id){ return state.db.assessments.find(a=>a.id===id); }
  function question(id){ return state.db.questions.find(q=>q.id===id); }
  function attemptsFor(studentId){ return state.db.attempts.filter(a=>a.studentId===studentId).sort((a,b)=>new Date(a.submittedAt)-new Date(b.submittedAt)); }
  function latestAttempt(studentId){ const a=attemptsFor(studentId); return a[a.length-1] || null; }
  function consentFor(studentId){ return state.db.consents.find(c=>c.studentId===studentId) || {studentId,assessment:false,academic:false,counselling:false}; }
  function scoreStudent(studentId){
    const att = latestAttempt(studentId);
    if(!att) return {percentile:0,score:0,trend:0,segment:'Not assessed',need:'Baseline assessment'};
    const arr=attemptsFor(studentId); const prev=arr.length>1?arr[arr.length-2]:null;
    const trend=prev?att.percentile-prev.percentile:0;
    let segment='Developing'; let need='Targeted practice';
    if(att.percentile>=90 && att.score>=80){segment='Academic Elite';need='Advanced challenge';}
    else if(trend>=10){segment='Fast Improver';need='Maintain momentum';}
    else if(att.score>=60 && att.avgTime<70){segment='High Potential, Careless';need='Accuracy routine';}
    else if(att.avgTime>75){segment='Accurate but Slow';need='Speed development';}
    return {percentile:att.percentile,score:att.score,trend,segment,need};
  }
  function counts(){ return {schools:state.db.schools.length,students:state.db.students.length,attempts:state.db.attempts.length,optins:state.db.consents.filter(c=>c.counselling).length,questions:state.db.questions.length,assessments:state.db.assessments.length}; }
  function fmtDate(v){ return new Date(v).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'}); }
  function avg(values){ return values.length ? Math.round(values.reduce((a,b)=>a+b,0)/values.length) : 0; }
  function downloadCsv(filename, rows){
    if(!rows.length) return alert('There is no data to export.');
    const headers=Object.keys(rows[0]);
    const csv=[headers.join(','),...rows.map(r=>headers.map(h=>'"'+String(r[h]??'').replaceAll('"','""')+'"').join(','))].join('\n');
    const blob=new Blob([csv],{type:'text/csv;charset=utf-8'}); const url=URL.createObjectURL(blob);
    const a=document.createElement('a'); a.href=url; a.download=filename; a.click(); URL.revokeObjectURL(url);
  }
  function toast(message){ const old=document.querySelector('.v2-toast'); if(old) old.remove(); const el=document.createElement('div'); el.className='v2-toast'; el.textContent=message; document.body.appendChild(el); setTimeout(()=>el.remove(),2800); }

  function renderLogin(){
    return `<div class="login-page"><div class="login-panel"><div class="brand login-brand"><div class="brand-mark">S</div><div><h1>ScholarOS</h1><p>Student Intelligence Platform</p></div></div><span class="badge gold">Version 2 connected pilot</span><h2>Turn every assessment into a learning action.</h2><p class="login-copy">Sign in to manage schools, build assessments, analyse students, create teacher interventions and capture only permissioned counselling opportunities.</p><form id="loginForm" class="login-form"><div class="field"><label>Email</label><input class="input" name="email" type="email" value="super@scholaros.in" required></div><div class="field"><label>Password</label><input class="input" name="password" type="password" value="demo123" required></div><button class="btn btn-primary btn-block">Sign in</button></form><div class="demo-logins"><p>One-click demo access</p>${state.db.users.map(u=>`<button class="demo-login" data-demo-user="${u.id}"><strong>${roleConfig[u.role].label}</strong><span>${u.name}</span></button>`).join('')}</div><div class="notice blue">Pilot data is stored in this browser. The Supabase schema in the repository is ready for server-backed tenancy and authentication.</div></div><div class="login-visual"><div class="login-stat"><span>Schools</span><strong>${state.db.schools.length}</strong></div><div class="login-stat"><span>Students</span><strong>${state.db.students.length}</strong></div><div class="login-stat"><span>Assessment attempts</span><strong>${state.db.attempts.length}</strong></div><div class="login-stat"><span>Permissioned opportunities</span><strong>${state.db.opportunities.length}</strong></div><div class="login-insight"><span class="eyebrow">What makes ScholarOS different</span><h3>Scores are not the final output.</h3><p>The product diagnoses misconceptions, identifies teachable groups, schedules retention checks and keeps academic evidence separate from marketing consent.</p></div></div></div>`;
  }

  render = function(){
    if(!state.session){ document.getElementById('app').innerHTML=renderLogin()+renderModal(); bind(); return; }
    const cfg=roleConfig[state.role],canSwitch=state.session.role==='super';
    document.getElementById('app').innerHTML=`<div class="app-shell"><aside class="sidebar" id="sidebar"><div class="brand"><div class="brand-mark">S</div><div><h1>ScholarOS</h1><p>Student Intelligence Platform</p></div></div>${canSwitch?`<div class="role-switcher"><label>VIEW PRODUCT AS</label><select id="roleSelect">${Object.entries(roleConfig).map(([k,v])=>`<option value="${k}" ${state.role===k?'selected':''}>${v.label}</option>`).join('')}</select></div>`:`<div class="signed-role"><span>${cfg.label}</span><strong>${esc(state.session.name)}</strong></div>`}<div class="nav-group-title">Workspace</div>${cfg.nav.map(([id,label,icon])=>`<button class="nav-btn ${state.page===id?'active':''}" data-page="${id}"><span class="nav-icon">${icon}</span>${label}</button>`).join('')}<div class="nav-group-title">Platform</div><button class="nav-btn" data-modal="quick"><span class="nav-icon">＋</span>Quick actions</button><button class="nav-btn" data-modal="privacy"><span class="nav-icon">⚿</span>Data principles</button><button class="nav-btn" data-action="logout"><span class="nav-icon">⇥</span>Sign out</button><div class="sidebar-footer">Version 2.0 Connected Pilot<br>Persistent browser data • CSV export<br><br>Backend-ready Supabase schema included.</div></aside><main class="main"><header class="topbar"><div class="top-actions"><button class="btn btn-outline mobile-menu" id="menuBtn">☰</button><div class="topbar-title"><h2>${cfg.label}</h2><p>${cfg.subtitle}</p></div></div><div class="top-actions"><button class="btn btn-outline btn-sm" data-modal="quick">＋ Quick action</button><div class="avatar">${state.session.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div></div></header><section class="content">${renderPage()}</section></main></div>${renderModal()}`;
    bind();
  };

  function superDashboard(){
    const c=counts(),active=state.db.schools.filter(s=>s.status==='Active').length,consentRate=Math.round(100*state.db.consents.filter(x=>x.assessment).length/Math.max(1,state.db.students.length)),calibration=Math.round(100*state.db.questions.filter(q=>q.status==='Calibrated').length/Math.max(1,state.db.questions.length));
    return `<div class="page-head"><div><span class="eyebrow">LIVE OPERATING VIEW</span><h3>Command centre</h3><p>Adoption, assessment quality, learning outcomes and permissioned opportunities across every school.</p></div><button class="btn btn-primary" data-modal="school">＋ Onboard school</button></div><div class="grid grid-4">${metric('▦',c.schools,'Schools in pipeline',`${active} active`)}${metric('♙',c.students,'Students mapped','Persistent records')}${metric('✓',c.attempts,'Tests completed','Live analytics')}${metric('◎',c.optins,'Counselling opt-ins','Consent verified','warn')}</div><div class="grid grid-3" style="margin-top:18px"><div class="card span-2"><div class="card-head"><div><h4 class="card-title">Assessment activity</h4><p class="card-sub">Completed attempts by recorded date</p></div>${badge('Live')}</div><div class="chart-bars">${state.db.attempts.slice(-12).map((a,i)=>`<div class="bar-col"><div class="bar ${i>7?'gold':''}" style="height:${45+(a.score*.7)}px"></div><div class="bar-label">${i+1}</div></div>`).join('')}</div></div><div class="card card-pad"><h4 class="card-title">Data readiness</h4><p class="card-sub">Can reports support reliable decisions?</p><div class="donut-wrap" style="margin:18px 0"><div class="donut"></div><div class="donut-label"><strong>${Math.round((consentRate+calibration)/2)}%</strong><span>ready</span></div></div><div class="list"><div><div class="metric-label">Assessment consent</div>${pct(consentRate)}</div><div><div class="metric-label">Question calibration</div>${pct(calibration)}</div><div><div class="metric-label">Students with repeat evidence</div>${pct(Math.round(100*state.db.students.filter(s=>attemptsFor(s.id).length>1).length/Math.max(1,state.db.students.length)))}</div></div></div></div><div class="grid grid-2" style="margin-top:18px"><div class="card"><div class="card-head"><div><h4 class="card-title">School pipeline</h4><p class="card-sub">Operational next actions</p></div><button class="btn btn-outline btn-sm" data-page="schools">Open all</button></div><div class="list card-pad">${state.db.schools.slice(0,4).map((s,i)=>`<div class="list-item"><div class="rank">${i+1}</div><div class="list-item-main"><h4>${esc(s.name)}</h4><p>${esc(s.city)} • ${s.students} students • ${s.consent}% consent</p></div>${badge(s.status)}</div>`).join('')}</div></div><div class="card"><div class="card-head"><div><h4 class="card-title">Academic segments</h4><p class="card-sub">Calculated from current attempt evidence</p></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Segment</th><th>Students</th><th>Academic action</th></tr></thead><tbody>${['Academic Elite','High Potential, Careless','Fast Improver','Accurate but Slow'].map(seg=>{const n=state.db.students.filter(s=>scoreStudent(s.id).segment===seg).length;const act={'Academic Elite':'Advanced cohort','High Potential, Careless':'Accuracy clinic','Fast Improver':'Maintain momentum','Accurate but Slow':'Timed practice'}[seg];return `<tr><td>${seg}</td><td>${n}</td><td>${act}</td></tr>`}).join('')}</tbody></table></div></div></div>`;
  }

  function schoolsV2(){ return `<div class="page-head"><div><h3>Partner schools</h3><p>Create isolated school tenants, track pilots and prepare assessment delivery.</p></div><div class="page-actions"><button class="btn btn-outline" data-action="export-schools">Export CSV</button><button class="btn btn-primary" data-modal="school">＋ Add school</button></div></div><div class="toolbar"><input class="input" id="schoolSearch" style="max-width:320px" placeholder="Search school or city"><span class="muted">${state.db.schools.length} school records</span></div><div class="card table-wrap"><table class="table"><thead><tr><th>School</th><th>Board</th><th>Grades</th><th>Students</th><th>Consent</th><th>Status</th><th>Coordinator</th></tr></thead><tbody id="schoolRows">${state.db.schools.map(s=>`<tr data-school-row="${(s.name+' '+s.city).toLowerCase()}"><td><strong>${esc(s.name)}</strong><div class="cell-sub">${esc(s.city)}</div></td><td>${esc(s.board||'—')}</td><td>${esc(s.grades)}</td><td>${s.students}</td><td>${s.consent||0}%</td><td>${badge(s.status)}</td><td>${esc(s.coordinator||'Unassigned')}</td></tr>`).join('')}</tbody></table></div>`; }

  function assessmentsV2(){ return `<div class="page-head"><div><h3>Assessment studio</h3><p>Create foundation, Olympiad, board-readiness or NEET assessments from your question bank.</p></div><button class="btn btn-primary" data-modal="assessment">＋ Create assessment</button></div><div class="grid grid-4">${metric('✓',state.db.assessments.filter(a=>a.status==='Live').length,'Live assessments','Student-ready')}${metric('▤',state.db.questions.length,'Questions available',`${state.db.questions.filter(q=>q.status==='Calibrated').length} calibrated`)}${metric('◷',avg(state.db.assessments.map(a=>a.duration))+' min','Average duration','Blueprint controlled')}${metric('◎',(state.db.questions.reduce((n,q)=>n+Number(q.discrimination||0),0)/Math.max(1,state.db.questions.length)).toFixed(2),'Avg discrimination','Question quality')}</div><div class="card" style="margin-top:18px"><div class="card-head"><div><h4 class="card-title">Assessment catalogue</h4><p class="card-sub">Publishing state, audience, delivery mode and recorded attempts</p></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Assessment</th><th>Grade</th><th>Type</th><th>Mode</th><th>Questions</th><th>Attempts</th><th>Status</th></tr></thead><tbody>${state.db.assessments.map(a=>`<tr><td><strong>${esc(a.name)}</strong><div class="cell-sub">Created ${fmtDate(a.createdAt)}</div></td><td>${a.grade}</td><td>${esc(a.type)}</td><td>${esc(a.mode)}</td><td>${a.questionIds.length}</td><td>${state.db.attempts.filter(x=>x.assessmentId===a.id).length}</td><td>${badge(a.status)}</td></tr>`).join('')}</tbody></table></div></div>`; }

  function questionsV2(){ return `<div class="page-head"><div><h3>Question bank</h3><p>Track curriculum tags, thinking skills, expected time and item-quality evidence.</p></div><div class="page-actions"><button class="btn btn-outline" data-action="export-questions">Export CSV</button><button class="btn btn-primary" data-modal="question">＋ Add question</button></div></div><div class="grid grid-4">${metric('▤',state.db.questions.length,'Total questions','Across subjects')}${metric('✓',state.db.questions.filter(q=>q.status==='Calibrated').length,'Calibrated','Ready to benchmark')}${metric('! ',state.db.questions.filter(q=>q.status==='Review').length,'Need review','Professor queue','warn')}${metric('◷',avg(state.db.questions.map(q=>q.expectedTime))+'s','Expected time','Per item')}</div><div class="card table-wrap" style="margin-top:18px"><table class="table"><thead><tr><th>Question</th><th>Subject</th><th>Topic</th><th>Skill</th><th>Difficulty</th><th>Time</th><th>Discrimination</th><th>Status</th></tr></thead><tbody>${state.db.questions.map(q=>`<tr><td class="question-cell"><strong>${esc(q.text)}</strong></td><td>${esc(q.subject)}</td><td>${esc(q.topic)}</td><td>${esc(q.skill)}</td><td>${badge(q.difficulty)}</td><td>${q.expectedTime}s</td><td>${Number(q.discrimination||0).toFixed(2)}</td><td>${badge(q.status)}</td></tr>`).join('')}</tbody></table></div>`; }

  function studentDirectoryV2(schoolId=null){ const records=state.db.students.filter(s=>!schoolId||s.schoolId===schoolId); return `<div class="page-head"><div><h3>Student directory</h3><p>Search performance, trend, academic need and parent permission separately.</p></div><div class="page-actions"><button class="btn btn-outline" data-action="export-students">Export CSV</button><button class="btn btn-primary" data-modal="students">Import students</button></div></div><div class="card table-wrap"><table class="table"><thead><tr><th>Student</th><th>School</th><th>Grade</th><th>Percentile</th><th>Trend</th><th>Segment</th><th>Counselling</th></tr></thead><tbody>${records.map(s=>{const sc=scoreStudent(s.id),co=consentFor(s.id);return `<tr><td><strong>${esc(s.name)}</strong><div class="cell-sub">${esc(s.parent)}</div></td><td>${esc(schoolName(s.schoolId))}</td><td>${s.grade}-${s.section}</td><td>${sc.percentile||'—'}</td><td><span class="trend ${sc.trend<0?'down':''}">${sc.trend>0?'+':''}${sc.trend}</span></td><td>${badge(sc.segment)}</td><td>${co.counselling?badge('Consent verified'):'No opt-in'}</td></tr>`}).join('')}</tbody></table></div>`; }

  function intelligenceV2(){ const eligible=state.db.students.filter(s=>consentFor(s.id).counselling); return `<div class="page-head"><div><h3>Student intelligence and opportunities</h3><p>Academic readiness determines the intervention. Parent permission determines whether a sales workflow may begin.</p></div><button class="btn btn-outline" data-action="export-leads">Export Odoo-ready CSV</button></div><div class="notice blue">A high percentile never creates a lead automatically. This pipeline contains only records with separate counselling consent.</div><div class="grid grid-4" style="margin-top:18px">${metric('★',state.db.students.filter(s=>scoreStudent(s.id).segment==='Academic Elite').length,'Academic Elite','Advanced learning')}${metric('↗',state.db.students.filter(s=>scoreStudent(s.id).segment==='Fast Improver').length,'Fast Improvers','Momentum')}${metric('⚠',state.db.students.filter(s=>scoreStudent(s.id).segment==='High Potential, Careless').length,'Potential, careless','Accuracy action')}${metric('☎',eligible.length,'Counselling permission','CRM eligible')}</div><div class="card" style="margin-top:18px"><div class="card-head"><div><h4 class="card-title">Permissioned opportunity pipeline</h4><p class="card-sub">Academic evidence, intent and next action in one operational view</p></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Student</th><th>School</th><th>Grade</th><th>Academic segment</th><th>Interest</th><th>Stage</th><th>Next action</th></tr></thead><tbody>${eligible.map(s=>{const sc=scoreStudent(s.id),op=state.db.opportunities.find(o=>o.studentId===s.id);return `<tr><td><strong>${esc(s.name)}</strong></td><td>${esc(schoolName(s.schoolId))}</td><td>${s.grade}</td><td>${badge(sc.segment)}</td><td>${esc(s.interest)}</td><td>${badge(op?.stage||'Consent verified')}</td><td><button class="btn ${op?'btn-outline':'btn-primary'} btn-sm" data-action="advance-opportunity" data-student="${s.id}">${esc(op?.nextAction||'Create opportunity')}</button></td></tr>`}).join('')}</tbody></table></div></div>`; }

  function consentV2(){ const total=state.db.students.length,n=k=>state.db.consents.filter(c=>c[k]).length; return `<div class="page-head"><div><h3>Consent and privacy</h3><p>Record each purpose independently and preserve an auditable verification timestamp.</p></div><button class="btn btn-primary" data-modal="consent">Record parent consent</button></div><div class="grid grid-4">${metric('⚿',n('assessment'),'Assessment consent',Math.round(100*n('assessment')/Math.max(1,total))+'%')}${metric('✉',n('academic'),'Academic updates',Math.round(100*n('academic')/Math.max(1,total))+'%')}${metric('☎',n('counselling'),'Counselling opt-in',Math.round(100*n('counselling')/Math.max(1,total))+'%')}${metric('♙',total-state.db.consents.length,'Consent pending','Follow-up required','warn')}</div><div class="card table-wrap" style="margin-top:18px"><table class="table"><thead><tr><th>Student</th><th>Parent</th><th>Assessment</th><th>Academic updates</th><th>Counselling</th><th>Verified</th></tr></thead><tbody>${state.db.students.map(s=>{const c=consentFor(s.id);return `<tr><td><strong>${esc(s.name)}</strong></td><td>${esc(s.parent)}</td><td>${c.assessment?badge('Consent verified'):'Pending'}</td><td>${c.academic?badge('Consent verified'):'No'}</td><td>${c.counselling?badge('Consent verified'):'No'}</td><td>${c.verifiedAt?fmtDate(c.verifiedAt):'—'}</td></tr>`}).join('')}</tbody></table></div>`; }

  function reportsV2(){ return `<div class="page-head"><div><h3>Reports and exports</h3><p>Generate useful outputs for students, parents, teachers, principals and your sales operations.</p></div></div><div class="grid grid-3"><div class="card card-pad report-card"><span class="report-icon">♙</span><h4>Student intelligence report</h4><p class="card-sub">Score, percentile, speed, mastery, errors, evidence and next actions.</p><button class="btn btn-outline btn-block" data-action="print-student">Open printable report</button></div><div class="card card-pad report-card"><span class="report-icon">✦</span><h4>Teacher intervention report</h4><p class="card-sub">Misconception clusters, student groups, lesson actions and retest dates.</p><button class="btn btn-outline btn-block" data-action="export-interventions">Export intervention CSV</button></div><div class="card card-pad report-card"><span class="report-icon">▦</span><h4>Principal benchmark report</h4><p class="card-sub">Grade, section, subject, participation and learning-health summary.</p><button class="btn btn-outline btn-block" data-action="print-principal">Open printable report</button></div><div class="card card-pad report-card"><span class="report-icon">☎</span><h4>Permissioned lead export</h4><p class="card-sub">Odoo-ready parent counselling records with consent and campaign context.</p><button class="btn btn-primary btn-block" data-action="export-leads">Export leads</button></div><div class="card card-pad report-card"><span class="report-icon">▤</span><h4>Question quality export</h4><p class="card-sub">Difficulty, expected time, discrimination, status and professor-review queue.</p><button class="btn btn-outline btn-block" data-action="export-questions">Export questions</button></div><div class="card card-pad report-card"><span class="report-icon">↺</span><h4>Pilot reset</h4><p class="card-sub">Restore the original Version 2 demonstration dataset in this browser.</p><button class="btn btn-soft btn-block" data-action="reset-demo">Reset demo data</button></div></div>`; }

  function adminDashboardV2(){ const c=counts(); return `<div class="page-head"><div><h3>Programme operations</h3><p>Control question quality, assessment delivery, student records and report turnaround.</p></div><button class="btn btn-primary" data-modal="assessment">＋ New assessment</button></div><div class="grid grid-4">${metric('✓',state.db.assessments.filter(a=>a.status==='Live').length,'Tests running','Ready now')}${metric('▤',c.questions,'Questions','Tagged and searchable')}${metric('♙',c.students,'Students','Across schools')}${metric('◷','Instant','Report turnaround','Computed on submit')}</div><div class="grid grid-2" style="margin-top:18px"><div class="card"><div class="card-head"><div><h4 class="card-title">Question quality queue</h4><p class="card-sub">Evidence requiring professor review</p></div><button class="btn btn-outline btn-sm" data-page="questions">Open bank</button></div><div class="list card-pad">${state.db.questions.filter(q=>q.status!=='Calibrated'||q.discrimination<.3).map(q=>`<div class="list-item"><div class="rank">${q.discrimination.toFixed(2)}</div><div class="list-item-main"><h4>${esc(q.topic)}</h4><p>${esc(q.text.slice(0,75))}...</p></div>${badge('Review')}</div>`).join('')||'<div class="empty-state">No question-quality alerts.</div>'}</div></div><div class="card card-pad"><h4 class="card-title">Professor’s operating discipline</h4><div class="action-plan"><div class="action-step"><div><strong>Diagnose before prescribing</strong><span>Separate knowledge, procedure, interpretation, speed and careless errors.</span></div></div><div class="action-step"><div><strong>Require repeated evidence</strong><span>Do not label a learner from one test or one difficult day.</span></div></div><div class="action-step"><div><strong>Close the learning loop</strong><span>Every weak topic needs practice, teacher action and a scheduled retest.</span></div></div></div></div></div>`; }

  function schoolDashboardV2(){ const schoolId=state.session.schoolId || 'sch1',school=state.db.schools.find(s=>s.id===schoolId)||state.db.schools[0],students=state.db.students.filter(s=>s.schoolId===school.id),scores=students.map(s=>scoreStudent(s.id)).filter(x=>x.percentile),needs=students.filter(s=>scoreStudent(s.id).percentile && scoreStudent(s.id).percentile<75).length,participation=Math.round(100*students.filter(s=>latestAttempt(s.id)).length/Math.max(1,students.length)); return `<div class="page-head"><div><span class="eyebrow">SCHOOL LEARNING HEALTH</span><h3>${esc(school.name)}</h3><p>See grade performance, high-potential students and evidence-backed teacher actions.</p></div><button class="btn btn-primary" data-action="print-principal">Download principal report</button></div><div class="grid grid-4">${metric('♙',students.length,'Students','Mapped roster')}${metric('✓',participation+'%','Participation','Current benchmark')}${metric('◎',avg(scores.map(x=>x.percentile))+'th','Median percentile','Current cohort')}${metric('✦',needs,'Need intervention','Teacher action','warn')}</div><div class="grid grid-3" style="margin-top:18px"><div class="card span-2"><div class="card-head"><div><h4 class="card-title">Grade and section performance</h4><p class="card-sub">Calculated from latest student attempts</p></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Cohort</th><th>Students</th><th>Assessed</th><th>Avg percentile</th><th>Immediate need</th></tr></thead><tbody>${[...new Set(students.map(s=>`${s.grade}-${s.section}`))].map(cohort=>{const arr=students.filter(s=>`${s.grade}-${s.section}`===cohort),assessed=arr.filter(s=>latestAttempt(s.id)),av=avg(assessed.map(s=>scoreStudent(s.id).percentile));return `<tr><td><strong>Grade ${cohort}</strong></td><td>${arr.length}</td><td>${assessed.length}</td><td>${av||'—'}</td><td>${av&&av<75?'Targeted support':'On track'}</td></tr>`}).join('')}</tbody></table></div></div><div class="card card-pad"><h4 class="card-title">Immediate attention</h4><div class="list" style="margin-top:14px">${students.filter(s=>scoreStudent(s.id).percentile&&scoreStudent(s.id).percentile<80).slice(0,4).map(s=>{const sc=scoreStudent(s.id);return `<div class="list-item"><div class="rank">${sc.percentile}</div><div class="list-item-main"><h4>${esc(s.name)}</h4><p>${esc(sc.need)}</p></div></div>`}).join('')||'<div class="empty-state">No immediate alerts.</div>'}</div></div></div>`; }

  function classesV2(){ const schoolId=state.session.schoolId || 'sch1',students=state.db.students.filter(s=>s.schoolId===schoolId),cohorts=[...new Set(students.map(s=>`${s.grade}-${s.section}`))]; return `<div class="page-head"><div><h3>Classes and sections</h3><p>Compare learning health without publishing a student ranking board.</p></div></div><div class="grid grid-3">${cohorts.map(c=>{const arr=students.filter(s=>`${s.grade}-${s.section}`===c),assessed=arr.filter(s=>latestAttempt(s.id)),av=avg(assessed.map(s=>scoreStudent(s.id).percentile));return `<div class="card card-pad"><div style="display:flex;justify-content:space-between"><h4 class="card-title">Grade ${c}</h4>${badge(av&&av<75?'Needs support':'On track')}</div><div class="metric-value">${av||0}th</div><p class="card-sub">Average percentile • ${arr.length} students • ${assessed.length} assessed</p><div style="margin-top:14px">${pct(av||0)}</div></div>`}).join('')}</div>`; }

  function interventionsV2(){ const schoolId=state.session.schoolId || 'sch1',rows=state.db.interventions.filter(x=>x.schoolId===schoolId); return `<div class="page-head"><div><h3>Teacher interventions</h3><p>Convert repeated evidence into small-group teaching, practice and retesting.</p></div><button class="btn btn-primary" data-modal="intervention">Create intervention</button></div><div class="notice green">Teachers remain in control. ScholarOS recommends groups and actions; it does not permanently label students.</div><div class="grid grid-2" style="margin-top:18px">${rows.map(i=>`<div class="card card-pad"><div style="display:flex;justify-content:space-between"><div><h4 class="card-title">${esc(i.title)}</h4><p class="card-sub">${i.studentIds.length} students • Retest ${fmtDate(i.retestDate)}</p></div>${badge(i.status)}</div><div class="student-chips">${i.studentIds.map(id=>`<span>${esc(state.db.students.find(s=>s.id===id)?.name||id)}</span>`).join('')}</div><div class="action-plan"><div class="action-step"><div><strong>15-minute diagnostic discussion</strong><span>Surface the exact misconception before reteaching.</span></div></div><div class="action-step"><div><strong>Contrast worked examples</strong><span>Compare correct logic with the most common wrong route.</span></div></div><div class="action-step"><div><strong>Close with a retest</strong><span>Require at least 80% before closing this intervention.</span></div></div></div></div>`).join('')||'<div class="card card-pad empty-state">No active interventions.</div>'}</div>`; }

  function studentDashboardV2(){ const s=currentStudent(),arr=attemptsFor(s.id),last=arr[arr.length-1],first=arr[0],sc=scoreStudent(s.id),gain=last&&first?last.percentile-first.percentile:0; return `<div class="page-head"><div><h3>Good afternoon, ${esc(s.name.split(' ')[0])}</h3><p>Your dashboard prioritises what to learn next, not only the marks you already received.</p></div><button class="btn btn-primary" data-page="tests">Take assessment</button></div><div class="analytics-hero"><div><span class="badge gold">Grade ${s.grade} • ${esc(s.interest)} interest</span><h3 style="margin-top:14px">${sc.segment}</h3><p>${sc.need}. Your next task is selected from actual response patterns and previous performance.</p></div><div class="score-ring"><div style="text-align:center"><strong>${last?.percentile||0}</strong><small>percentile</small></div></div></div><div class="grid grid-4" style="margin-top:18px">${metric('✓',(last?.score||0)+'%','Latest score',last?'Recorded':'Not assessed')}${metric('◎',(last?.percentile||0)+'th','Percentile',sc.segment)}${metric('◷',(last?.avgTime||0)+'s','Time / question','Current average')}${metric('↗',(gain>0?'+':'')+gain,'Percentile gain',arr.length+' tests')}</div><div class="grid grid-2" style="margin-top:18px"><div class="card card-pad"><h4 class="card-title">Your next three actions</h4>${studentActions(s.id)}</div><div class="card card-pad"><h4 class="card-title">Performance history</h4><div class="history-chart">${arr.map((a,i)=>`<div class="history-point"><span style="height:${Math.max(12,a.percentile)}%"></span><strong>${a.percentile}</strong><small>T${i+1}</small></div>`).join('')||'<div class="empty-state">Complete your first assessment.</div>'}</div></div></div>`; }

  function studentActions(studentId){ const a=latestAttempt(studentId); if(!a) return '<div class="empty-state">Take the baseline assessment to generate your plan.</div>'; const as=assessment(a.assessmentId),wrong=as.questionIds.map(question).filter(q=>a.answers[q.id]!==q.answer),actions=wrong.slice(0,3).map(q=>`<div class="action-step"><div><strong>${esc(q.topic)}</strong><span>${q.errorType} error: review the explanation, complete 8 controlled questions and retest in 7 days.</span></div></div>`); if(actions.length<3) actions.push(`<div class="action-step"><div><strong>Retention check</strong><span>Revisit one mastered topic after 21 days to confirm durable learning.</span></div></div>`); return `<div class="action-plan">${actions.join('')}</div>`; }

  function testsV2(){ const s=currentStudent(); if(state.testComplete && state.lastAttemptId) return analyticsV2(); const available=state.db.assessments.filter(a=>a.status==='Live' && (a.grade===s.grade || a.type==='Foundation')); if(!state.activeAssessmentId || !assessment(state.activeAssessmentId)) state.activeAssessmentId=available[0]?.id; const as=assessment(state.activeAssessmentId); if(!as) return `<div class="empty-state card card-pad">No live assessment is available for your grade.</div>`; const qs=as.questionIds.map(question).filter(Boolean),q=qs[state.testIndex],selected=state.answers[q.id]; return `<div class="test-shell"><div class="page-head"><div><h3>${esc(as.name)}</h3><p>${as.type} • ${as.mode} • ${as.duration} minutes</p></div><div class="timer">Autosave active</div></div><div class="assessment-picker"><label>Assessment</label><select class="select" id="assessmentSelect">${available.map(x=>`<option value="${x.id}" ${x.id===as.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="test-meta">${badge(q.subject)}${badge(q.topic)}${badge(q.difficulty)}<span class="badge">${q.skill}</span><span class="badge">Question ${state.testIndex+1} of ${qs.length}</span></div><div class="question-card"><div class="question-number">Question ${state.testIndex+1}</div><div class="question-text">${esc(q.text)}</div>${q.options.map((o,i)=>`<div class="option ${selected===i?'selected':''}" data-v2-answer="${i}" data-question="${q.id}"><div class="option-key">${String.fromCharCode(65+i)}</div><div>${esc(o)}</div></div>`).join('')}</div><div class="test-footer"><button class="btn btn-outline" data-v2-test="prev" ${state.testIndex===0?'disabled':''}>← Previous</button><div class="test-progress">${pct(Math.round(100*Object.keys(state.answers).length/qs.length))}<small>${Object.keys(state.answers).length}/${qs.length} answered</small></div><button class="btn btn-primary" data-v2-test="${state.testIndex===qs.length-1?'submit':'next'}">${state.testIndex===qs.length-1?'Submit assessment':'Save & next →'}</button></div></div>`; }

  function finishAttempt(){ const s=currentStudent(),as=assessment(state.activeAssessmentId),qs=as.questionIds.map(question).filter(Boolean),correct=qs.filter(q=>Number(state.answers[q.id])===q.answer).length,score=Math.round(100*correct/qs.length),previous=attemptsFor(s.id),percentile=Math.min(99,Math.max(20,Math.round(score*.72+35+(previous.length*2)))),times={}; qs.forEach(q=>times[q.id]=state.answerTimes[q.id]||q.expectedTime); const attempt={id:uid('at'),assessmentId:as.id,studentId:s.id,submittedAt:new Date().toISOString(),score,correct,total:qs.length,percentile,avgTime:avg(Object.values(times)),answers:{...state.answers},times}; state.db.attempts.push(attempt); state.lastAttemptId=attempt.id; saveDb(); state.testComplete=true; state.page='analytics'; }

  function analyticsV2(){ const s=currentStudent(),a=state.db.attempts.find(x=>x.id===state.lastAttemptId)||latestAttempt(s.id); if(!a) return `<div class="empty-state card card-pad">Complete an assessment to generate analytics.</div>`; const as=assessment(a.assessmentId),qs=as.questionIds.map(question).filter(Boolean),wrong=qs.filter(q=>Number(a.answers[q.id])!==q.answer),topics=qs.map(q=>({q,ok:Number(a.answers[q.id])===q.answer,time:a.times[q.id]||q.expectedTime})),causes={Conceptual:0,Procedural:0,Interpretation:0,Recall:0,'Time pressure':0}; wrong.forEach(q=>causes[q.errorType]=(causes[q.errorType]||0)+1); topics.filter(x=>x.time>x.q.expectedTime*1.3).forEach(()=>causes['Time pressure']++); return `<div class="page-head"><div><h3>Performance intelligence</h3><p>This report explains what happened, why it happened and what should happen next.</p></div><div class="page-actions"><button class="btn btn-outline" data-action="print-student">Print report</button><button class="btn btn-primary" data-action="restart-test">Take another attempt</button></div></div><div class="analytics-hero"><div><span class="badge gold">Assessment complete • ${fmtDate(a.submittedAt)}</span><h3 style="margin-top:14px">${a.correct}/${a.total} correct • ${a.percentile}th percentile</h3><p>${a.score>=80?'Strong current mastery. Focus on making the performance repeatable under different question formats.':'Your report has identified recoverable gaps. Repair the misconception before increasing difficulty.'}</p></div><div class="score-ring"><div style="text-align:center"><strong>${a.score}%</strong><small>score</small></div></div></div><div class="grid grid-4" style="margin-top:18px">${metric('◎',a.percentile+'th','Percentile',a.percentile>=90?'Top 10%':'Current cohort')}${metric('✓',a.score+'%','Accuracy',a.score>=80?'Strong':'Developing')}${metric('◷',a.avgTime+'s','Avg response time',a.avgTime<70?'Efficient':'Needs pacing')}${metric('✦',wrong.length,'Priority questions','Action ready')}</div><div class="grid grid-2" style="margin-top:18px"><div class="card card-pad"><h4 class="card-title">Topic evidence</h4><p class="card-sub">Correctness and time compared with expected solving time</p><div class="topic-evidence">${topics.map(x=>`<div><span>${esc(x.q.topic)}</span><strong class="${x.ok?'ok':'bad'}">${x.ok?'Correct':'Review'}</strong><small>${x.time}s / ${x.q.expectedTime}s expected</small></div>`).join('')}</div></div><div class="card card-pad"><h4 class="card-title">Error diagnosis</h4><p class="card-sub">Marks and time lost by cause</p><div class="list" style="margin-top:16px">${Object.entries(causes).map(([k,v])=>`<div><div style="display:flex;justify-content:space-between"><span class="metric-label">${k}</span><strong>${v}</strong></div>${pct(Math.min(100,v*28))}</div>`).join('')}</div></div></div><div class="grid grid-2" style="margin-top:18px"><div class="card card-pad"><h4 class="card-title">Professor’s action plan</h4>${studentActions(s.id)}</div><div class="card"><div class="card-head"><div><h4 class="card-title">Question review</h4><p class="card-sub">Correct logic and your response</p></div></div><div class="list card-pad">${qs.map((q,i)=>{const chosen=a.answers[q.id],ok=Number(chosen)===q.answer;return `<div class="question-review"><div class="rank">${i+1}</div><div><h4>${esc(q.topic)} ${ok?'✓':'— Review'}</h4><p>${ok?'Correct. '+esc(q.explanation):`Your answer: ${chosen===undefined?'Not answered':esc(q.options[chosen])}. ${esc(q.explanation)}`}</p></div></div>`}).join('')}</div></div></div>`; }

  function practiceV2(){ const s=currentStudent(),a=latestAttempt(s.id); if(!a)return `<div class="empty-state card card-pad">Take an assessment to generate adaptive practice.</div>`; const as=assessment(a.assessmentId),wrong=as.questionIds.map(question).filter(q=>Number(a.answers[q.id])!==q.answer),cards=(wrong.length?wrong:as.questionIds.map(question).slice(0,2)).map((q,i)=>`<div class="card card-pad"><span class="badge ${i?'gold':'red'}">${i?'Priority '+(i+1):'Priority 1'}</span><h4>${esc(q.topic)}</h4><p class="card-sub">${q.errorType} repair • 8 questions • ${12+i*4} minutes</p><div style="margin:14px 0">${pct(i?0:25)}</div><button class="btn btn-outline btn-block" data-action="practice-toast">${i?'Start':'Continue'}</button></div>`); cards.push(`<div class="card card-pad"><span class="badge green">Retention</span><h4>Mastered-topic check</h4><p class="card-sub">5 questions • due in 7 days</p><div style="margin:14px 0">${pct(100)}</div><button class="btn btn-soft btn-block">Scheduled</button></div>`); return `<div class="page-head"><div><h3>Adaptive practice plan</h3><p>Practice is selected from evidence, not assigned as random extra work.</p></div></div><div class="grid grid-3">${cards.join('')}</div>`; }

  renderSuper = function(){ if(state.page==='schools')return schoolsV2(); if(state.page==='assessments')return assessmentsV2(); if(state.page==='questions')return questionsV2(); if(state.page==='intelligence')return intelligenceV2(); if(state.page==='consent')return consentV2(); if(state.page==='reports')return reportsV2(); if(state.page==='roadmap')return roadmapV2(); return superDashboard(); };
  renderAdmin = function(){ if(state.page==='assessments')return assessmentsV2(); if(state.page==='questions')return questionsV2(); if(state.page==='students')return studentDirectoryV2(); if(state.page==='intelligence')return intelligenceV2(); if(state.page==='reports')return reportsV2(); return adminDashboardV2(); };
  renderSchool = function(){ if(state.page==='students')return studentDirectoryV2(state.session.schoolId||'sch1'); if(state.page==='reports')return reportsV2(); if(state.page==='interventions')return interventionsV2(); if(state.page==='classes')return classesV2(); return schoolDashboardV2(); };
  renderStudent = function(){ if(state.page==='tests')return testsV2(); if(state.page==='analytics')return analyticsV2(); if(state.page==='practice')return practiceV2(); if(state.page==='certificates')return certificatesPage(); return studentDashboardV2(); };

  function roadmapV2(){ return `<div class="page-head"><div><h3>Production roadmap</h3><p>Version 2 makes the pilot operational. The next release connects the same workflows to hosted data and school deployment.</p></div>${badge('Version 2 live')}</div><div class="grid grid-2"><div class="card card-pad"><h4 class="card-title">V2 — Operational pilot</h4><div class="action-plan"><div class="action-step"><div><strong>Persistent multi-role data</strong><span>Schools, students, questions, assessments, attempts, consents and opportunities survive refresh.</span></div></div><div class="action-step"><div><strong>Working academic workflow</strong><span>Create records, complete assessments, compute analytics and export operational data.</span></div></div><div class="action-step"><div><strong>Consent-safe lead pipeline</strong><span>Only explicitly permissioned students enter the counselling export.</span></div></div></div></div><div class="card card-pad"><h4 class="card-title">V3 — Supabase production backend</h4><div class="action-plan"><div class="action-step"><div><strong>Google and email authentication</strong><span>Server-backed sessions and role permissions.</span></div></div><div class="action-step"><div><strong>Tenant isolation with RLS</strong><span>Every school sees only its own students and reports.</span></div></div><div class="action-step"><div><strong>Storage and event pipeline</strong><span>Images, question imports, autosave events and generated reports.</span></div></div></div></div><div class="card card-pad"><h4 class="card-title">V4 — OMR and parent portal</h4><div class="action-plan"><div class="action-step"><div><strong>OMR sheets and scan import</strong><span>Offline school tests feed the same analytics engine.</span></div></div><div class="action-step"><div><strong>OTP parent reports</strong><span>Bilingual reports, acknowledgement and counselling booking.</span></div></div><div class="action-step"><div><strong>Odoo API sync</strong><span>Permissioned opportunities, campaign attribution and counsellor updates.</span></div></div></div></div><div class="card card-pad"><h4 class="card-title">V5 — Intelligence moat</h4><div class="action-plan"><div class="action-step"><div><strong>Question psychometrics</strong><span>Difficulty, discrimination, distractor behaviour and calibration confidence.</span></div></div><div class="action-step"><div><strong>Forgetting and retention model</strong><span>Schedule revision based on demonstrated decay.</span></div></div><div class="action-step"><div><strong>District benchmarking</strong><span>Privacy-safe benchmarks with verified recognition.</span></div></div></div></div></div>`; }

  renderModal = function(){
    if(!state.modal)return''; const close=`<button class="icon-btn" data-close>✕</button>`;
    if(state.modal==='school')return `<div class="modal-backdrop"><div class="modal"><div class="modal-head"><div><strong>Onboard a school</strong><div class="card-sub">Create the pilot tenant and operating record</div></div>${close}</div><div class="modal-body"><form id="schoolFormV2" class="form-grid"><div class="field"><label>School name</label><input class="input" name="name" required></div><div class="field"><label>City</label><input class="input" name="city" required></div><div class="field"><label>Board</label><select class="select" name="board"><option>CBSE</option><option>ICSE</option><option>State</option><option>Other</option></select></div><div class="field"><label>Grades</label><select class="select" name="grades"><option>8–10</option><option>8–12</option><option>9–12</option><option>11–12</option></select></div><div class="field"><label>Estimated students</label><input class="input" name="students" type="number" value="200" min="1"></div><div class="field"><label>Coordinator</label><input class="input" name="coordinator" placeholder="Name or role"></div><div class="span-2"><button class="btn btn-primary btn-block">Create school tenant</button></div></form></div></div></div>`;
    if(state.modal==='assessment')return `<div class="modal-backdrop"><div class="modal"><div class="modal-head"><div><strong>Create assessment</strong><div class="card-sub">Publish from an outcome, audience and question blueprint</div></div>${close}</div><div class="modal-body"><form id="assessmentFormV2" class="form-grid"><div class="field span-2"><label>Assessment name</label><input class="input" name="name" value="Grade 10 Foundation Benchmark" required></div><div class="field"><label>Grade</label><select class="select" name="grade"><option>8</option><option>9</option><option selected>10</option><option>11</option><option>12</option></select></div><div class="field"><label>Type</label><select class="select" name="type"><option>Foundation</option><option>Olympiad</option><option>Board Readiness</option><option>NEET</option><option>JEE</option></select></div><div class="field"><label>Mode</label><select class="select" name="mode"><option>CBT</option><option>CBT + OMR</option><option>OMR</option></select></div><div class="field"><label>Duration (minutes)</label><input class="input" name="duration" type="number" value="30" min="5"></div><div class="field span-2"><label>Question selection</label><div class="checkbox-grid">${state.db.questions.map(q=>`<label><input type="checkbox" name="questionIds" value="${q.id}" checked> ${esc(q.subject)} — ${esc(q.topic)}</label>`).join('')}</div></div><div class="span-2"><button class="btn btn-primary btn-block">Publish live assessment</button></div></form></div></div></div>`;
    if(state.modal==='question')return `<div class="modal-backdrop"><div class="modal modal-wide"><div class="modal-head"><div><strong>Add question</strong><div class="card-sub">Tag the concept, thinking skill, time and answer logic</div></div>${close}</div><div class="modal-body"><form id="questionFormV2" class="form-grid"><div class="field"><label>Subject</label><input class="input" name="subject" required></div><div class="field"><label>Topic</label><input class="input" name="topic" required></div><div class="field"><label>Difficulty</label><select class="select" name="difficulty"><option>Easy</option><option selected>Medium</option><option>Hard</option><option>Very Hard</option></select></div><div class="field"><label>Thinking skill</label><select class="select" name="skill"><option>Recall</option><option>Understanding</option><option selected>Application</option><option>Analysis</option></select></div><div class="field span-2"><label>Question</label><textarea class="textarea" name="text" required></textarea></div>${[0,1,2,3].map(i=>`<div class="field"><label>Option ${String.fromCharCode(65+i)}</label><input class="input" name="option${i}" required></div>`).join('')}<div class="field"><label>Correct option</label><select class="select" name="answer"><option value="0">A</option><option value="1">B</option><option value="2">C</option><option value="3">D</option></select></div><div class="field"><label>Expected time (seconds)</label><input class="input" name="expectedTime" type="number" value="60"></div><div class="field span-2"><label>Explanation</label><textarea class="textarea" name="explanation" required></textarea></div><div class="span-2"><button class="btn btn-primary btn-block">Add to question bank</button></div></form></div></div></div>`;
    if(state.modal==='students')return `<div class="modal-backdrop"><div class="modal"><div class="modal-head"><div><strong>Import students</strong><div class="card-sub">Paste CSV rows: name, grade, section, school name, parent, phone, interest</div></div>${close}</div><div class="modal-body"><form id="studentImportForm"><textarea class="textarea import-box" name="csv">Aarav Shetty,10,A,Green Valley School,Deepa Shetty,9000011111,NEET\nIshita Rao,9,B,Green Valley School,Manoj Rao,9000011112,Undecided</textarea><button class="btn btn-primary btn-block" style="margin-top:14px">Import student records</button></form></div></div></div>`;
    if(state.modal==='consent')return `<div class="modal-backdrop"><div class="modal"><div class="modal-head"><div><strong>Record parent consent</strong><div class="card-sub">Each purpose is independent and unchecked by default</div></div>${close}</div><div class="modal-body"><form id="consentFormV2" class="form-grid"><div class="field span-2"><label>Student</label><select class="select" name="studentId">${state.db.students.map(s=>`<option value="${s.id}">${esc(s.name)} — ${esc(s.parent)}</option>`).join('')}</select></div><label class="consent-row span-2"><input type="checkbox" name="assessment"> <span><strong>Assessment participation</strong><small>Take tests and receive the academic report.</small></span></label><label class="consent-row span-2"><input type="checkbox" name="academic"> <span><strong>Academic communication</strong><small>Reminders, practice and future assessments.</small></span></label><label class="consent-row span-2"><input type="checkbox" name="counselling"> <span><strong>Programme counselling</strong><small>Grade 11, NEET or JEE information and parent calls.</small></span></label><div class="span-2"><button class="btn btn-primary btn-block">Verify and save consent</button></div></form></div></div></div>`;
    if(state.modal==='intervention')return `<div class="modal-backdrop"><div class="modal"><div class="modal-head"><div><strong>Create teacher intervention</strong><div class="card-sub">Group students around a specific teachable need</div></div>${close}</div><div class="modal-body"><form id="interventionFormV2" class="form-grid"><div class="field span-2"><label>Intervention title</label><input class="input" name="title" placeholder="Example: Graph interpretation" required></div><div class="field span-2"><label>Students</label><div class="checkbox-grid">${state.db.students.filter(s=>s.schoolId===(state.session.schoolId||'sch1')).map(s=>`<label><input type="checkbox" name="studentIds" value="${s.id}"> ${esc(s.name)}</label>`).join('')}</div></div><div class="field span-2"><label>Retest date</label><input class="input" type="date" name="retestDate" required></div><div class="span-2"><button class="btn btn-primary btn-block">Create intervention</button></div></form></div></div></div>`;
    if(state.modal==='privacy')return `<div class="modal-backdrop"><div class="modal"><div class="modal-head"><strong>Product data principles</strong>${close}</div><div class="modal-body"><div class="action-plan"><div class="action-step"><div><strong>Academic evidence is not a sales permission</strong><span>Performance and parent marketing choices remain separate.</span></div></div><div class="action-step"><div><strong>No deterministic labels</strong><span>Report current readiness and uncertainty, never “will clear” or “cannot clear”.</span></div></div><div class="action-step"><div><strong>Tenant boundaries matter</strong><span>School users see their institution; cross-school benchmarks are aggregated.</span></div></div><div class="action-step"><div><strong>Every insight must produce an action</strong><span>A report without a learning or teaching action is incomplete.</span></div></div></div></div></div></div>`;
    return `<div class="modal-backdrop"><div class="modal"><div class="modal-head"><strong>Quick actions</strong>${close}</div><div class="modal-body"><div class="role-preview"><button class="role-card" data-modal="school">▦<strong>Onboard school</strong><p>Create a tenant and pilot record.</p></button><button class="role-card" data-modal="assessment">✓<strong>Create assessment</strong><p>Publish from the question bank.</p></button><button class="role-card" data-modal="question">▤<strong>Add question</strong><p>Grow the calibrated bank.</p></button><button class="role-card" data-page="tests">▶<strong>Take assessment</strong><p>Experience the student flow.</p></button></div></div></div></div>`;
  };

  bind = function(){
    document.querySelectorAll('[data-page]').forEach(el=>el.onclick=()=>{state.page=el.dataset.page;state.modal=null;render()});
    document.querySelectorAll('[data-modal]').forEach(el=>el.onclick=()=>{state.modal=el.dataset.modal;render()});
    document.querySelectorAll('[data-close]').forEach(el=>el.onclick=()=>{state.modal=null;render()});
    document.querySelectorAll('[data-demo-user]').forEach(el=>el.onclick=()=>{const u=state.db.users.find(x=>x.id===el.dataset.demoUser);setSession(u);state.role=u.role;state.page='dashboard';render()});
    const lf=document.getElementById('loginForm'); if(lf)lf.onsubmit=e=>{e.preventDefault();const f=new FormData(lf),u=state.db.users.find(x=>x.email.toLowerCase()===String(f.get('email')).toLowerCase()&&x.password===f.get('password'));if(!u)return alert('Invalid demo credentials. Use the one-click accounts below.');setSession(u);state.role=u.role;render()};
    const rs=document.getElementById('roleSelect');if(rs)rs.onchange=e=>{state.role=e.target.value;state.page='dashboard';localStorage.setItem('scholaros-role',state.role);render()};
    const mb=document.getElementById('menuBtn');if(mb)mb.onclick=()=>document.getElementById('sidebar').classList.toggle('open');
    const ss=document.getElementById('schoolSearch'); if(ss)ss.oninput=e=>document.querySelectorAll('[data-school-row]').forEach(r=>r.style.display=r.dataset.schoolRow.includes(e.target.value.toLowerCase())?'':'none');
    document.querySelectorAll('[data-v2-answer]').forEach(el=>el.onclick=()=>{const qid=el.dataset.question;if(state.answers[qid]===undefined)state.answerTimes[qid]=Math.max(1,Math.round((Date.now()-state.questionStartedAt)/1000));state.answers[qid]=Number(el.dataset.v2Answer);render()});
    document.querySelectorAll('[data-v2-test]').forEach(el=>el.onclick=()=>{const as=assessment(state.activeAssessmentId),qs=as.questionIds.map(question).filter(Boolean),q=qs[state.testIndex],action=el.dataset.v2Test;if(action==='prev'){state.testIndex=Math.max(0,state.testIndex-1);state.questionStartedAt=Date.now()}if(action==='next'){if(state.answers[q.id]===undefined)return alert('Choose an answer before continuing.');state.testIndex=Math.min(qs.length-1,state.testIndex+1);state.questionStartedAt=Date.now()}if(action==='submit'){if(state.answers[q.id]===undefined)return alert('Choose an answer before submitting.');if(Object.keys(state.answers).length<qs.length&&!confirm('Some questions are unanswered. Submit anyway?'))return;finishAttempt()}render()});
    const aps=document.getElementById('assessmentSelect'); if(aps)aps.onchange=e=>{state.activeAssessmentId=e.target.value;state.testIndex=0;state.answers={};state.answerTimes={};state.questionStartedAt=Date.now();render()};
    const sf=document.getElementById('schoolFormV2');if(sf)sf.onsubmit=e=>{e.preventDefault();const f=new FormData(sf);state.db.schools.push({id:uid('sch'),name:f.get('name'),city:f.get('city'),board:f.get('board'),grades:f.get('grades'),students:Number(f.get('students')),status:'Pilot',coordinator:f.get('coordinator')||'Unassigned',consent:0,createdAt:new Date().toISOString()});saveDb();state.modal=null;state.page='schools';toast('School tenant created');render()};
    const af=document.getElementById('assessmentFormV2');if(af)af.onsubmit=e=>{e.preventDefault();const f=new FormData(af),qids=f.getAll('questionIds');if(!qids.length)return alert('Select at least one question.');state.db.assessments.push({id:uid('as'),name:f.get('name'),grade:f.get('grade'),type:f.get('type'),mode:f.get('mode'),duration:Number(f.get('duration')),status:'Live',schoolIds:state.db.schools.map(s=>s.id),questionIds:qids,createdAt:new Date().toISOString()});saveDb();state.modal=null;state.page='assessments';toast('Assessment published');render()};
    const qf=document.getElementById('questionFormV2');if(qf)qf.onsubmit=e=>{e.preventDefault();const f=new FormData(qf);state.db.questions.push({id:uid('q'),subject:f.get('subject'),topic:f.get('topic'),difficulty:f.get('difficulty'),skill:f.get('skill'),expectedTime:Number(f.get('expectedTime')),errorType:f.get('skill')==='Recall'?'Recall':'Conceptual',text:f.get('text'),options:[0,1,2,3].map(i=>f.get('option'+i)),answer:Number(f.get('answer')),explanation:f.get('explanation'),status:'Review',discrimination:0});saveDb();state.modal=null;state.page='questions';toast('Question added for professor review');render()};
    const sif=document.getElementById('studentImportForm');if(sif)sif.onsubmit=e=>{e.preventDefault();const f=new FormData(sif),lines=String(f.get('csv')).trim().split(/\n+/),created=[];lines.forEach(line=>{const [name,grade,section,school,parent,phone,interest]=line.split(',').map(x=>x.trim());if(!name||!grade)return;let sch=state.db.schools.find(s=>s.name.toLowerCase()===String(school).toLowerCase())||state.db.schools[0];const rec={id:uid('st'),name,grade,section:section||'A',schoolId:sch.id,parent:parent||'Parent',phone:phone||'',interest:interest||'Undecided',status:'Active'};state.db.students.push(rec);created.push(rec)});saveDb();state.modal=null;state.page='students';toast(`${created.length} students imported`);render()};
    const cf=document.getElementById('consentFormV2');if(cf)cf.onsubmit=e=>{e.preventDefault();const f=new FormData(cf),sid=f.get('studentId');state.db.consents=state.db.consents.filter(c=>c.studentId!==sid);state.db.consents.push({studentId:sid,assessment:f.has('assessment'),academic:f.has('academic'),counselling:f.has('counselling'),verifiedAt:new Date().toISOString()});saveDb();state.modal=null;state.page='consent';toast('Consent record verified');render()};
    const intf=document.getElementById('interventionFormV2');if(intf)intf.onsubmit=e=>{e.preventDefault();const f=new FormData(intf),ids=f.getAll('studentIds');if(!ids.length)return alert('Select at least one student.');state.db.interventions.push({id:uid('in'),schoolId:state.session.schoolId||'sch1',title:f.get('title'),studentIds:ids,status:'Open',retestDate:f.get('retestDate')});saveDb();state.modal=null;state.page='interventions';toast('Teacher intervention created');render()};
    document.querySelectorAll('[data-action]').forEach(el=>el.onclick=()=>{
      const a=el.dataset.action;
      if(a==='logout'){setSession(null);state.role='super';state.page='dashboard';render();}
      if(a==='restart-test'){state.testComplete=false;state.lastAttemptId=null;state.answers={};state.answerTimes={};state.testIndex=0;state.questionStartedAt=Date.now();state.page='tests';render();}
      if(a==='practice-toast')toast('Practice session started — completion will be tracked in Version 3.');
      if(a==='advance-opportunity'){const sid=el.dataset.student;let op=state.db.opportunities.find(o=>o.studentId===sid);if(!op){state.db.opportunities.push({id:uid('op'),studentId:sid,stage:'Counselling requested',owner:'Unassigned',nextAction:'Assign counsellor',createdAt:new Date().toISOString()});toast('Permissioned opportunity created');}else{const stages=['Counselling requested','Parent contacted','Meeting booked','Programme considered','Closed'],i=Math.min(stages.length-1,stages.indexOf(op.stage)+1);op.stage=stages[i];op.nextAction=i===stages.length-1?'Completed':'Advance to '+stages[i+1];toast('Opportunity stage updated');}saveDb();render();}
      if(a==='export-schools')downloadCsv('scholaros-schools.csv',state.db.schools.map(s=>({school:s.name,city:s.city,board:s.board,grades:s.grades,students:s.students,status:s.status,coordinator:s.coordinator,consent_percent:s.consent})));
      if(a==='export-students')downloadCsv('scholaros-students.csv',state.db.students.map(s=>{const sc=scoreStudent(s.id),c=consentFor(s.id);return {student:s.name,school:schoolName(s.schoolId),grade:s.grade,section:s.section,parent:s.parent,phone:s.phone,interest:s.interest,percentile:sc.percentile,segment:sc.segment,assessment_consent:c.assessment,academic_updates:c.academic,counselling_consent:c.counselling}}));
      if(a==='export-questions')downloadCsv('scholaros-question-quality.csv',state.db.questions.map(q=>({subject:q.subject,topic:q.topic,difficulty:q.difficulty,skill:q.skill,expected_time:q.expectedTime,discrimination:q.discrimination,status:q.status,question:q.text})));
      if(a==='export-leads')downloadCsv('scholaros-odoo-permissioned-leads.csv',state.db.students.filter(s=>consentFor(s.id).counselling).map(s=>{const sc=scoreStudent(s.id),op=state.db.opportunities.find(o=>o.studentId===s.id),c=consentFor(s.id);return {student_name:s.name,parent_name:s.parent,parent_phone:s.phone,school:schoolName(s.schoolId),grade:s.grade,interest:s.interest,academic_segment:sc.segment,latest_percentile:sc.percentile,stage:op?.stage||'Consent verified',next_action:op?.nextAction||'Create opportunity',consent_verified_at:c.verifiedAt}}));
      if(a==='export-interventions')downloadCsv('scholaros-teacher-interventions.csv',state.db.interventions.map(i=>({school:schoolName(i.schoolId),intervention:i.title,students:i.studentIds.map(id=>state.db.students.find(s=>s.id===id)?.name).join('; '),status:i.status,retest_date:i.retestDate})));
      if(a==='print-student'){state.role='student';state.page='analytics';render();setTimeout(()=>window.print(),150)}
      if(a==='print-principal'){state.role='school';state.page='dashboard';render();setTimeout(()=>window.print(),150)}
      if(a==='reset-demo'&&confirm('Reset all Version 2 pilot data in this browser?')){state.db=clone(defaultDb);saveDb();toast('Demo data restored');render();}
    });
  };

  saveDb();
  render();
})();
