# Evidara Phase 1 - QA Compatibility Fix (2026-08-27)

## Error reproduced
The local launcher successfully installed dependencies and validated the environment, then failed inside `qa:regression` because the legacy public-student smoke test still required two behaviours that Phase 1 intentionally disables:

- the landing page linking `/products/`
- the landing page marketing independent-student commerce

## Fix
- Updated `scripts/post8-public-student-smoke.mjs` so those two checks now assert that the parked public-commerce messaging is absent during Phase 1.
- Kept the remainder of the legacy test intact so the store, referral, checkout and migration engines are still verified as retained in source for future restoration.
- Added `scripts/phase1-launch-smoke.mjs` to the start of `qa:regression`, so the Phase-1 visibility/access policy is now verified on every local preflight.
- No CSS, institution workflow, question importer, study-resource behaviour, database schema or hidden future-engine implementation was removed by this fix.

## Verification
- `phase1-launch-smoke.mjs`: 42/42 passed.
- corrected `post8-public-student-smoke.mjs`: 18/18 passed.
- complete `npm run qa:regression`: passed with exit code 0.

`TEST_EVIDARA.bat` still runs the complete `qa:final` sequence on the user's PC, including typecheck, lint, regression and the Next.js production build before starting the local server.
