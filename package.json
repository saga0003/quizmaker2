{
  "name": "scholaros",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css data.js shell.js super.js admin-school.js student.js interactions.js dist/"
  }
}
