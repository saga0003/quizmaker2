{
  "name": "scholaros",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css v2.css data.js shell.js super.js admin-school.js student.js interactions.js v2.js dist/"
  }
}
